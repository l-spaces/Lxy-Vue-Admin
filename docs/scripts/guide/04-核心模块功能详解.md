---
title: 核心模块功能详解
description: 认证授权、路由权限、状态管理、组件系统等核心模块详解
lastUpdated: true
---

# 核心模块功能详解

## 目录

- [1. 认证授权模块](#1-认证授权模块)
  - [1.1 登录认证](#11-登录认证)
  - [1.2 Token 管理](#12-token-管理)
  - [1.3 第三方登录](#13-第三方登录)
- [2. 路由权限模块](#2-路由权限模块)
  - [2.1 路由分类](#21-路由分类)
  - [2.2 路由守卫](#22-路由守卫)
  - [2.3 动态路由生成](#23-动态路由生成)
- [3. 状态管理模块](#3-状态管理模块)
  - [3.1 Store 分类](#31-store-分类)
  - [3.2 字典 Store](#32-字典-store)
  - [3.3 租户 Store](#33-租户-store)
- [4. 组件系统](#4-组件系统)
  - [4.1 组件分类](#41-组件分类)
  - [4.2 上传组件](#42-上传组件)
  - [4.3 字典组件](#43-字典组件)
  - [4.4 树形组件](#44-树形组件)
- [5. 国际化模块](#5-国际化模块)
  - [5.1 语言包结构](#51-语言包结构)
  - [5.2 使用方法](#52-使用方法)
- [6. 字典管理模块](#6-字典管理模块)
  - [6.1 字典类型](#61-字典类型)
  - [6.2 字典使用](#62-字典使用)
- [7. 租户管理模块](#7-租户管理模块)
  - [7.1 租户功能](#71-租户功能)
  - [7.2 租户切换](#72-租户切换)
- [8. 文件上传模块](#8-文件上传模块)
  - [8.1 上传类型](#81-上传类型)
  - [8.2 OSS 配置](#82-oss-配置)
- [9. 工作流模块](#9-工作流模块)
  - [9.1 流程定义](#91-流程定义)
  - [9.2 流程实例](#92-流程实例)
  - [9.3 任务管理](#93-任务管理)

---

## 1. 认证授权模块

### 1.1 登录认证

**支持的登录方式**：

| 登录方式 | 说明 | 组件 |
|----------|------|------|
| 账号密码登录 | 用户名 + 密码 | `login.vue` |
| 验证码登录 | 手机号 + 验证码 | `code-login.vue` |
| 二维码登录 | 扫码登录 | `qrcode-login.vue` |
| 第三方登录 | OAuth 授权 | `oauth-login.vue` |

**登录参数**：

```typescript
interface SimpleLoginParams {
  clientId?: string;        // 客户端 ID
  grantType: GrantType;     // 授权类型
  tenantId: string;         // 租户 ID
  username: string;         // 用户名
  password: string;         // 密码
  code?: string;            // 验证码
  uuid?: string;            // 验证码 ID
}
```

**登录实现**：

```typescript
// store/auth.ts
async function authLogin(params: LoginAndRegisterParams) {
  loginLoading.value = true;
  try {
    // 1. 调用登录接口
    const { access_token } = await loginApi(params);
    
    // 2. 存储 Token
    accessStore.setAccessToken(access_token);
    accessStore.setRefreshToken(access_token);
    
    // 3. 获取用户信息
    const userInfo = await fetchUserInfo();
    userStore.setUserInfo(userInfo);
    
    // 4. 设置权限
    accessStore.setAccessCodes(userInfo.permissions);
    
    // 5. 跳转首页
    await router.push(preferences.app.defaultHomePath);
    
    // 6. 显示成功通知
    window.notification.success({
      description: `${$t('authentication.loginSuccessDesc')}:${userInfo?.realName}`,
      title: $t('authentication.loginSuccess'),
    });
  } finally {
    loginLoading.value = false;
  }
}
```

### 1.2 Token 管理

**Token 存储**：

- **Access Token**：用于接口鉴权
- **Refresh Token**：用于刷新 Access Token

**Token 使用**：

```typescript
// utils/http/index.ts - beforeRequest
const token = accessStore.accessToken;
if (config.withToken && token) {
  config.headers.Authorization = `Bearer ${token}`;
}
```

**Token 失效处理**：

```typescript
// 响应拦截器中处理 401
if (response.status === UNAUTHORIZED_CODE) {
  handleUnauthorizedLogout();
}
```

### 1.3 第三方登录

**支持的第三方平台**：

- 钉钉
- 微信
- QQ
- GitHub
- Gitee

**OAuth 登录流程**：

```typescript
// 绑定第三方账号
export function authBinding(source: string, tenantId: string) {
  return alovaInstance.get<string>(`/auth/binding/${source}`, {
    params: { domain: window.location.host, tenantId },
  });
}

// 取消绑定
export function authUnbinding(id: string) {
  return alovaInstance.deleteWithMsg<void>(`/auth/unlock/${id}`);
}

// OAuth 回调
export function authCallback(data: OAuthLoginParams) {
  return alovaInstance.post<void>('/auth/social/callback', data);
}
```

---

## 2. 路由权限模块

### 2.1 路由分类

**核心路由（Core Routes）**：

| 路由 | 路径 | 说明 |
|------|------|------|
| 登录页 | `/auth/login` | 用户登录 |
| 404 页面 | `/:pathMatch(.*)*` | 页面不存在 |
| 403 页面 | `/403` | 无权限 |
| 500 页面 | `/500` | 服务器错误 |

**动态路由（Access Routes）**：

- 从后端动态获取
- 根据用户权限生成
- 支持菜单权限控制

### 2.2 路由守卫

**守卫类型**：

| 守卫 | 功能 |
|------|------|
| 通用守卫 | 页面加载进度、路由记录 |
| 权限守卫 | Token 检查、动态路由生成 |

**权限守卫实现**：

```typescript
// router/guard.ts
function setupAccessGuard(router: Router) {
  router.beforeEach(async (to, from) => {
    const accessStore = useAccessStore();
    const userStore = useUserStore();
    const authStore = useAuthStore();

    // 1. 核心路由直接放行
    if (coreRouteNames.includes(to.name as string)) {
      return true;
    }

    // 2. Token 检查
    if (!accessStore.accessToken) {
      return { path: LOGIN_PATH, query: { redirect: to.fullPath } };
    }

    // 3. 已生成路由直接放行
    if (accessStore.isAccessChecked) {
      return true;
    }

    // 4. 生成动态路由
    const [userInfo, routeInfo] = await Promise.all([
      userStore.userInfo || authStore.fetchUserInfo(),
      generateAccess({ router, routes: accessRoutes }),
    ]);

    // 5. 保存路由状态
    accessStore.setAccessMenus(routeInfo.accessibleMenus);
    accessStore.setAccessRoutes(routeInfo.accessibleRoutes);
    accessStore.setIsAccessChecked(true);

    return redirectPath;
  });
}
```

### 2.3 动态路由生成

**后端路由转 Vben 路由**：

```typescript
// router/access.ts
function backMenuToVbenMenu(menuList: Menu[]): RouteRecordStringComponent[] {
  return menuList.map((menu) => {
    // 处理外链
    if (/^https?:\/\//.test(menu.path)) {
      menu.component = 'Link';
    }
    
    // 处理内嵌 iframe
    if (menu.meta?.link && menu.component === 'InnerLink') {
      menu.component = 'IFrameView';
    }

    return {
      component: menu.component,
      meta: {
        hideInMenu: menu.hidden,
        icon: menu.meta?.icon,
        keepAlive: !menu.meta?.noCache,
        title: menu.meta?.title,
      },
      name: menu.name,
      path: menu.path,
    };
  });
}
```

**组件映射**：

| 后端组件名 | 前端组件 |
|------------|----------|
| `Layout` | `BasicLayout` |
| `ParentView` | 空组件 |
| `InnerLink` | `IFrameView` |
| `Link` | `BasicLayout` (外链) |
| 其他 | `/${menu.component}` |

---

## 3. 状态管理模块

### 3.1 Store 分类

| Store | 说明 | 文件路径 |
|-------|------|----------|
| useAuthStore | 认证状态 | `store/auth.ts` |
| useUserStore | 用户状态 | `@vben/stores` |
| useAccessStore | 权限状态 | `@vben/stores` |
| useDictStore | 字典状态 | `store/dict.ts` |
| useTenantStore | 租户状态 | `store/tenant.ts` |
| useNotifyStore | 通知状态 | `store/notify.ts` |

### 3.2 字典 Store

**功能**：

- 缓存字典数据
- 提供字典 Options
- 避免重复请求

**核心实现**：

```typescript
// store/dict.ts
export const useDictStore = defineStore('app-dict', () => {
  // 字典缓存 Map
  const dictOptionsMap = reactive(new Map<string, DictOption[]>());
  
  // 请求状态缓存
  const dictRequestCache = reactive(new Map<string, Promise<DictData[]>>());

  // 获取字典 Options
  function getDictOptions(dictName: string): DictOption[] {
    if (!dictOptionsMap.has(dictName)) {
      dictOptionsMap.set(dictName, []);
    }
    return dictOptionsMap.get(dictName)!;
  }

  // 设置字典数据
  function setDictInfo(dictName: string, dictValue: DictData[]) {
    const options = dictToOptions(dictValue);
    dictOptionsMap.set(dictName, options);
  }

  // 清空缓存
  function resetCache() {
    dictRequestCache.clear();
    dictOptionsMap.clear();
  }

  return { getDictOptions, setDictInfo, resetCache };
});
```

### 3.3 租户 Store

**功能**：

- 管理租户列表
- 租户切换状态
- 租户功能开关

```typescript
// store/tenant.ts
export const useTenantStore = defineStore('app-tenant', () => {
  const checked = ref(false);           // 是否已选中租户
  const tenantEnable = ref(true);       // 是否开启租户功能
  const tenantList = ref<TenantOption[]>([]); // 租户列表

  // 初始化获取租户信息
  async function initTenant() {
    const { tenantEnabled, voList } = await tenantListApi();
    tenantEnable.value = tenantEnabled;
    tenantList.value = voList;
  }

  // 设置选中状态
  function setChecked(_checked: boolean) {
    checked.value = _checked;
  }

  return { checked, tenantEnable, tenantList, initTenant, setChecked };
});
```

---

## 4. 组件系统

### 4.1 组件分类

**基础组件**：

来自 Antdv Next，通过适配器统一使用方式。

**业务组件**：

| 组件 | 路径 | 功能 |
|------|------|------|
| 上传组件 | `components/upload/` | 图片/文件上传 |
| 字典组件 | `components/dict/` | 字典数据展示 |
| 树形组件 | `components/tree/` | 树形选择 |
| 富文本 | `components/tinymce/` | 富文本编辑 |
| 图片裁剪 | `components/cropper/` | 图片裁剪 |

### 4.2 上传组件

**支持的上传类型**：

- **ImageUpload**：图片上传
- **FileUpload**：文件上传

**核心 Hook**：

```typescript
// components/upload/src/hook.ts
export function useUpload(props: BaseUploadProps, emit: UploadEmits) {
  const innerFileList = ref<UploadFile[]>([]);

  // 自定义文件名
  function transformFilename(cb: UploadFile) {
    return props.customFilename?.(cb) ?? cb.response.fileName;
  }

  // 自定义缩略图
  function transformThumbUrl(cb: UploadFile) {
    return props.customThumbUrl?.(cb) ?? cb.response.url;
  }

  return { innerFileList, transformFilename, transformThumbUrl };
}
```

**使用示例**：

```vue
<script setup lang="ts">
import { ImageUpload } from '#/components/upload';

const fileList = ref<string>('');
</script>

<template>
  <ImageUpload 
    v-model="fileList" 
    :max-count="3" 
    accept="image/*"
  />
</template>
```

### 4.3 字典组件

**功能**：

- 自动加载字典数据
- 支持 Select、Radio、Checkbox 等组件
- 缓存字典数据避免重复请求

**使用示例**：

```vue
<script setup lang="ts">
import { Dict } from '#/components/dict';
</script>

<template>
  <!-- 字典 Select -->
  <Dict dictType="system_user_sex" component="Select" v-model="form.sex" />
  
  <!-- 字典 Radio -->
  <Dict dictType="system_user_sex" component="Radio" v-model="form.sex" />
  
  <!-- 字典 Checkbox -->
  <Dict dictType="system_user_sex" component="Checkbox" v-model="form.sexList" />
</template>
```

### 4.4 树形组件

**功能**：

- 树形数据展示
- 树形选择器
- 菜单选择表格

**使用示例**：

```vue
<script setup lang="ts">
import { TreeSelect } from '#/components/tree';

const selectedKeys = ref<string[]>([]);
const treeData = ref<TreeDataItem[]>([]);
</script>

<template>
  <TreeSelect 
    v-model="selectedKeys"
    :tree-data="treeData"
    placeholder="请选择"
  />
</template>
```

---

## 5. 国际化模块

### 5.1 语言包结构

```
locales/
├── langs/
│   ├── zh-CN/
│   │   ├── common.json      # 通用文本
│   │   ├── component.json   # 组件文本
│   │   ├── demos.json       # 演示文本
│   │   ├── http.json        # HTTP 相关文本
│   │   ├── menu.json        # 菜单文本
│   │   ├── page.json        # 页面文本
│   │   └── pages.json       # 页面集合文本
│   └── en-US/
│       └── ...              # 英文翻译
└── index.ts
```

### 5.2 使用方法

**在 TypeScript 中使用**：

```typescript
import { $t } from '#/locales';

// 使用翻译
const title = $t('page.dashboard.title');
const buttonText = $t('common.button.submit');
```

**在 Vue 模板中使用**：

```vue
<template>
  <div>{{ $t('page.dashboard.title') }}</div>
  <a-button>{{ $t('common.button.submit') }}</a-button>
</template>
```

**切换语言**：

```typescript
import { usePreferences } from '@vben/preferences';

const { updatePreferences } = usePreferences();

// 切换为英文
updatePreferences({ app: { locale: 'en-US' } });

// 切换为中文
updatePreferences({ app: { locale: 'zh-CN' } });
```

---

## 6. 字典管理模块

### 6.1 字典类型

**系统内置字典**：

| 字典类型 | 说明 |
|----------|------|
| `sys_common_status` | 系统通用状态 |
| `sys_normal_disable` | 正常/停用 |
| `sys_user_sex` | 用户性别 |
| `sys_show_hide` | 显示状态 |
| `sys_yes_no` | 是/否 |
| `sys_notice_type` | 通知类型 |
| `sys_notice_status` | 通知状态 |
| `sys_oper_type` | 操作类型 |
| `oss_access_policy` | OSS 权限桶类型 |
| `wf_business_status` | 业务状态 |
| `wf_form_type` | 表单类型 |
| `wf_task_status` | 任务状态 |

### 6.2 字典使用

**在表单中使用**：

```typescript
// views/system/user/data.ts
import { Dict } from '#/components/dict';

export const querySchema = () => {
  return [
    {
      field: 'sex',
      label: '性别',
      component: 'Select',
      componentProps: {
        options: () => Dict.getOptions('sys_user_sex'),
      },
    },
  ];
};
```

**在表格中使用**：

```typescript
// views/system/user/data.ts
export const columns: VxeGridColumns<User> = [
  {
    field: 'sex',
    title: '性别',
    width: 80,
    slots: {
      default: ({ row }) => {
        return h(Dict, {
          dictType: 'sys_user_sex',
          value: row.sex,
        });
      },
    },
  },
];
```

---

## 7. 租户管理模块

### 7.1 租户功能

**功能特性**：

- 多租户隔离
- 租户管理员切换
- 租户套餐管理
- 租户域名绑定

### 7.2 租户切换

**顶部租户切换组件**：

```vue
<script setup lang="ts">
import { TenantToggle } from '#/components/tenant-toggle';
</script>

<template>
  <TenantToggle />
</template>
```

**租户状态管理**：

```typescript
// store/tenant.ts
const tenantStore = useTenantStore();

// 初始化租户
await tenantStore.initTenant();

// 切换租户
await tenantStore.setChecked(true);
```

---

## 8. 文件上传模块

### 8.1 上传类型

| 类型 | 组件 | 说明 |
|------|------|------|
| 图片上传 | `ImageUpload` | 支持图片预览、裁剪 |
| 文件上传 | `FileUpload` | 支持多文件、进度显示 |

### 8.2 OSS 配置

**支持的存储服务**：

| 服务 | 说明 |
|------|------|
| 本地存储 | 服务器本地存储 |
| MinIO | 开源对象存储 |
| 阿里云 OSS | 阿里云对象存储 |
| 腾讯云 COS | 腾讯云对象存储 |
| 七牛云 | 七牛云存储 |

**配置管理**：

- 路径：`/system/oss-config`
- 支持多配置切换
- 动态配置存储策略

---

## 9. 工作流模块

### 9.1 流程定义

**功能**：

- 流程设计器
- 流程部署
- 流程版本管理

**核心文件**：

| 文件 | 说明 |
|------|------|
| `views/workflow/processDefinition/` | 流程定义页面 |
| `views/workflow/design.vue` | 流程设计器 |
| `api/workflow/definition/` | 流程定义 API |

### 9.2 流程实例

**功能**：

- 流程实例查询
- 流程实例管理
- 流程变量管理

**核心文件**：

| 文件 | 说明 |
|------|------|
| `views/workflow/processInstance/` | 流程实例页面 |
| `api/workflow/instance/` | 流程实例 API |

### 9.3 任务管理

**功能**：

- 待办任务
- 已办任务
- 任务审批
- 任务转办

**核心文件**：

| 文件 | 说明 |
|------|------|
| `views/workflow/task/taskWaiting.vue` | 待办任务 |
| `views/workflow/task/taskFinish.vue` | 已办任务 |
| `views/workflow/task/myDocument.vue` | 我的文档 |
| `api/workflow/task/` | 任务 API |

---

**文档版本：** v1.0  
**最后更新：** 2026-04-06  
**维护者：** Lxy-Vue-Admin Team
